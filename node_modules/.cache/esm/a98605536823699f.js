let School;_682‍.x([["createSchool",()=>createSchool],["getSchool",()=>getSchool]]);_682‍.w("../models",[["School",["School"],function(v){School=v}]]);

       const createSchool = async (req, res) => {
  try {
    const school = await School.create({
      ...req.body,
    })
    return res.status(200).json({ school: 'school created success!', data: school })
  } catch (err) {
    return res.status(500).json({ error: 'Something went wrong!' })
  }
}

       const getSchool = async (req, res) => {
  const { id } = req.params;
  try {
    const school = await School.findOne({
      where: {
        id: id
      }
    })
    return res.status(200).json({ data: school })
  } catch (err) {
    throw err
  }
}