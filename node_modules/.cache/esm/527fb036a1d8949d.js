let Teacher,Topic;_a1d‍.x([["createTopic",()=>createTopic],["getTopic",()=>getTopic],["updateTopic",()=>updateTopic],["deleteTopic",()=>deleteTopic]]);_a1d‍.w("../models",[["Teacher",["Teacher"],function(v){Teacher=v}],["Topic",["Topic"],function(v){Topic=v}]]);

       const createTopic = async (req, res) => {
  try {
    const topic = await Topic.create({
      ...req.body,
    })
    return res.status(200).json({ topic: 'teacher created success!', data: topic })
  } catch (err) {
    return res.status(500).json({ error: 'Something went wrong!' })
  }
}

       const getTopic = async (req, res) => {
  const { id } = req.params;
  try {
    const topic = await Topic.findOne({
      where: {
        id: id
      }
    })
    return res.status(200).json({ data: topic })
  } catch (err) {
    throw err
  }
}

       const updateTopic = async (req, res) => {
  try {
    const { id } = req.params;
    await Topic.update(req.body, {
      where: {
        id: id
      }
    })

    const topic = await Topic.findOne({
      where: {
        id: id
      }
    })
    if (!topic) {
      return res.status(201).json({ message: "topic not found" })
    }

    return  res.status(200).json({ message:"topic updated success!", data: topic })
  } catch (err) {
    _a1d‍.g.console.log("err", err)
  }
}

       const deleteTopic = async (req, res) => {
  try {
    const { id } = req.params;
    await Teacher.destroy({
      where: {
        id: id
      }
    })
    return  res.status(200).json({ message: "topic delete success!" })
  } catch(err) {
    _a1d‍.g.console.log("err", err)
  }
}