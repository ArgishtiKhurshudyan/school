let multer;_e61‍.x([["multerMiddleware",()=>multerMiddleware]]);_e61‍.w("multer",[["default",["multer"],function(v){multer=v}]]);


const upload = multer().any()
       const multerMiddleware =  async (req, res, next) => {
  try{
    await upload(req, res, (err)=>{
      if (err instanceof multer.MulterError) {
        return res.status(400).json({ message: "multer error" })
      } else if(err) {
        return res.status(400).json({ message: "something error" })
      }
      next()
    })
  }catch(err) {
    _e61‍.g.console.log(err)
  }
}

