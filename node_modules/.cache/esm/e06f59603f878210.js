let Teacher;_3f8‍.x([["createTeacher",()=>createTeacher],["getTeacher",()=>getTeacher]]);_3f8‍.w("../models",[["Teacher",["Teacher"],function(v){Teacher=v}]]);

       const createTeacher = async (req, res) => {
  // console.log("schoolid", req.school.id)
  try {

    const teacher = await Teacher.create({
      ...req.body,
      school_id:req.school.id
    })

    return res.status(200).json({teacher: 'teacher created success!', data: teacher})

  } catch (err) {
    return res.status(500).json({errorMessage: 'Something went wrong!'})
  }
}

       const getTeacher = async (req, res) => {
  const {id} = req.params;
  try {
    const teacher = await Teacher.findOne({
      where: {
        id: id
      }
    })
    return res.status(200).json({data: teacher})
  } catch (err) {
    throw err
  }
}