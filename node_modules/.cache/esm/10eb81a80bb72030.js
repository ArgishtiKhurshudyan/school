let Floor;_0bb‍.x([["createFloor",()=>createFloor],["getFloor",()=>getFloor]]);_0bb‍.w("../models",[["Floor",["Floor"],function(v){Floor=v}]]);

       const createFloor = async (req, res) => {
  try {
    const floor = await Floor.create({
      ...req.body,
    })
    return res.status(200).json({ floor: 'floor created success!', data: floor })
  } catch (err) {
    return res.status(500).json({ error: 'Something went wrong!' })
  }
}

       const getFloor = async (req, res) => {
  const { id } = req.params;
  try {
    const floor = await Floor.findOne({
      where: {
        id: id
      }
    })
    return res.status(200).json({ data: floor })
  } catch (err) {
    throw err
  }
}