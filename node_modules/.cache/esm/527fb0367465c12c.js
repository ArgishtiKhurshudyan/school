let Topic;_746‍.x([["createTopic",()=>createTopic],["getTopic",()=>getTopic]]);_746‍.w("../models",[["Topic",["Topic"],function(v){Topic=v}]]);

       const createTopic = async (req, res) => {
  try {
    const topic = await Topic.create({
      ...req.body,
    })
    return res.status(200).json({ topic: 'teacher created success!', data: topic })
  } catch (err) {
    return res.status(500).json({ error: 'Something went wrong!' })
  }
}

       const getTopic = async (req, res) => {
  const { id } = req.params;
  try {
    const topic = await Topic.findOne({
      where: {
        id: id
      }
    })
    return res.status(200).json({ data: topic })
  } catch (err) {
    throw err
  }
}